class NotificationTileModel{
  String title, subtitle, time,iconPath;
  NotificationTileModel({
    required this.title,
    required this.subtitle,
    required this.time,
    required this.iconPath
  });
}